= field_set do
  = hidden_field_tag :bank_statement_id, resource.id
  = f.referenced_association :cash, source: :pointables
  = f.input :number
  = f.input :started_at
  = f.input :stopped_at
  = f.custom_fields
  -# TODO support items on new record ?
  - unless resource.new_record?
    #items-form{href: new_backend_bank_statement_path, data: {refresh: new_backend_bank_statement_path, refresh_mode: :update, with: '#bank_statement_id'}}
      -# TODO update the items when the cash field changes
      = render 'items_form'

= f.attachments_field_set